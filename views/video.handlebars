{{#section 'head'}}
{{> default }}
{{> embeddable}}
<meta property="og:image" content="{{thumbnailUrl}}">
{{/section}}

<video poster="{{thumbnailUrl}}" autoplay loop controls>
    <source src="{{fileUrl}}" type="{{fileType}}/{{fileFormat}}">
</video>