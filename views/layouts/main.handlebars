<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    {{!-- file type based metadata tags --}}

    {{{ _sections.head }}}

    <style>
        html {
            height: 100%;
        }

        html,
        body {
            margin: 0;
            padding: 0;
            width: 100%;
            background-color: #090B10;
        }

        body {
            padding: 0px !important;
            font-size: 14px;
            font-family: "Open Sans", "Helvetica Neue", Helvetica, Arial, sans-serif;
            min-height: 100%;
            position: relative;
            line-height: 1.43;
            height: 100%;

        }

        .container {
            -webkit-overflow-scrolling: touch;
            text-align: center;
            padding: 0px 5px;
        }

        .content {
            display: inline-block;
            text-align: center;
            min-width: 250px;
            padding: 2em;
        }

        .box {
            position: relative;
            background-color: #0F111A;
            margin-top: 10px;
            box-shadow: 0 1px 2px rgba(0, 0, 0, 0.1);
            padding: 2em;
            color: white;
            border-bottom: .25em solid #06070A;
            border-radius: .5em;
        }

        .btn {
            padding: .75em;
            border: none;
            background-color: rgb(0, 221, 11);
            color: white;
            border-radius: .5em;
            border-bottom: .25em solid green;
            font-size: 20px;
        }

        .btn:hover {
            background-color: rgb(0, 161, 8);
            color: white;
            border-bottom: .25em solid green;
        }
    </style>

</head>

<body>

    <div class="container">
        <div class="content" style="max-width: 100em; max-height: 90em; min-width: 50em;">

            <div class="media" style="width: 100%;">
                {{{body}}}
            </div>

            <div id="everything-but-video">
                <div class="box " id="footer-content">

                    <div class="title">
                        <h1>{{fileName}}</h1>
                    </div>
                    <button class="btn" style="width:100%"><i class="fa fa-download"></i> Download</button>

                </div>
            </div>

        </div>
    </div>


</body>

</html>